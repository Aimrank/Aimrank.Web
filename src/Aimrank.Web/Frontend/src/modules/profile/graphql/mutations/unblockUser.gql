mutation unblockUser($userId: Uuid!) {
  unblockUser(command: {blockedUserId: $userId}) {
    query {
      friendship(userId: $userId) {
        isAccepted
        invitingUserId
        blockingUsersIds
      }
    }
  }
}
