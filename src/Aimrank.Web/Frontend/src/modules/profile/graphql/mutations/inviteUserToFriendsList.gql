mutation inviteUserToFriendsList($userId: Uuid!) {
  inviteUserToFriendsList(command: {invitedUserId: $userId}) {
    query {
      friendship(userId: $userId) {
        isAccepted
        invitingUserId
        blockingUsersIds
      }
    }
  }
}