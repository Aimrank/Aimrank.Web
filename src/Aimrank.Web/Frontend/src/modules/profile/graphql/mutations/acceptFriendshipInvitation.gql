mutation acceptFriendshipInvitation($userId: Uuid!) {
  acceptFriendshipInvitation(command: {invitingUserId: $userId}) {
    query {
      friendship(userId: $userId) {
        isAccepted
        invitingUserId
        blockingUsersIds
      }
    }
  }
}
