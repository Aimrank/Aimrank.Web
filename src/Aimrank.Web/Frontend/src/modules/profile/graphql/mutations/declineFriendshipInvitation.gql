mutation declineFriendshipInvitation($userId: Uuid!) {
  declineFriendshipInvitation(command: {invitingUserId: $userId}) {
    query {
      friendship(userId: $userId) {
        isAccepted
        invitingUserId
        blockingUsersIds
      }
    }
  }
}
