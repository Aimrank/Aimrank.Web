@using System.Text.Json
@model Aimrank.Web.ViewModels.InitialAppStateViewModel

<script>
window.initialAppState = @Html.Raw(JsonSerializer.Serialize(Model, new JsonSerializerOptions {PropertyNamingPolicy = JsonNamingPolicy.CamelCase}))
</script>